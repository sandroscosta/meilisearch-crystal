# This configuration file was generated by `ameba --gen-config`
# on 2026-08-11 20:49:44 UTC using Ameba version 1.6.4.
#
# The point is for the user to remove these configuration records
# one by one as the reported problems are removed from the code base.
#
# For more details on any individual rule, run
# `ameba --ignore-config --only RuleName`.

Version: "1.6.4"

Lint/UselessAssign:
  # Ameba 1.6.4 sees typed arguments to the Resource `field` macros as local
  # assignments. Keep the rule enabled for ordinary assignments.
  ExcludeTypeDeclarations: true

Lint/SpecEqWithBoolOrNilLiteral:
  Excluded:
  - spec/meilisearch-crystal_spec.cr
